import React, {useContext, useEffect} from 'react'import App from 'next/app'import '../styles/App/App.scss'import {AnimatePresence} from 'framer-motion'import Header from "../components/header/Header";import AnimationContextProvider, {AnimationContext} from "../context/AnimationContext";import Overlay from "../components/landing/Overlay";import Cursor from "../components/scrollers/Cursor";class MyApp extends App {    getMouseCoords = (e) => {        console.log(e.clientX);        console.log(23)    };    componentDidMount(){        // window.addEventListener("mousemove", this.getMouseCoords)    }    render() {        const {Component, pageProps, router} = this.props;        return (            <AnimationContextProvider>                <Header/>                <Cursor/>                <Overlay/>                <AnimatePresence exitBeforeEnter>                    <Component {...pageProps} key={router.route}/>                </AnimatePresence>            </AnimationContextProvider>        )    }}export default MyApp